# Editor / OS
.vs/
.idea/
.vscode/
*.user
.DS_Store
Thumbs.db

# Node (only used for optional local tooling; the game itself needs none)
node_modules/

# Local build/output folders (the game ships as source — no build step)
dist/
